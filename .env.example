# Required for Netlify consent API (min 32 characters).
# Set the same value in Netlify: Site settings → Environment variables → CONSENT_SECRET
CONSENT_SECRET=replace-with-a-random-string-at-least-32-chars-long
